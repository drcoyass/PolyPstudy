name: Daily PubMed Research Sync

on:
  schedule:
    - cron: '0 0 * * *' # 日本時間の午前9時に毎日実行
  workflow_dispatch: # 手動でも動かせるようにする

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/set-up-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install deep-translator

      - name: Run sync script
        run: python sync_all_research.py

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/latest_papers.json
          git commit -m "Auto-update: Sync latest PubMed research ($(date +'%Y-%m-%d'))" || echo "No changes to commit"
          git push
